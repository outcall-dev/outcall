[package]
name = "outcall-api"
version = "0.1.18"
edition = "2024"
description = "Shared types and constants for Outcall"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
