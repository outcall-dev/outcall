[package]
name = "outcall-api"
version = "0.1.35"
edition = "2024"
description = "Shared types and constants for Outcall"
license = "Apache-2.0"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
